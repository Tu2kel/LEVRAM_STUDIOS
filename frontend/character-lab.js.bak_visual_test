console.log("Character Lab standalone page loaded");

document.addEventListener("DOMContentLoaded", () => {
  const status = document.getElementById("character-lab-status");
  if (status) status.textContent = "Character Lab is now running as its own page.";
});
